# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'TelephoneBook' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'
  pod 'Firebase/Core’
  pod 'Firebase/Database’
  # Pods for TelephoneBook

  target 'TelephoneBookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TelephoneBookUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
